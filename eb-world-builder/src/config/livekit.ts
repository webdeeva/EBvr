// LiveKit configuration
export const LIVEKIT_URL = process.env.REACT_APP_LIVEKIT_URL!;

// Note: Token generation is handled by utils/livekitToken.ts
// which uses Supabase Edge Functions in production